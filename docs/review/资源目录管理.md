# 斗蛐蛐 · 资源目录管理

> 读者是美术与程序。本文是资源交接，不是玩法功能文档。
> 玩法以 [碰撞逻辑与配置](碰撞逻辑与配置_develop_v1.md)、[交互逻辑](交互逻辑_develop_v0.md)、[道具系统](道具系统_develop_v0.md) 为准。
> 范围：Scene / Prefab 归属、运行时图路径、替换槽。不含跳跃公式、投放、胜负、联机。

当前工程里的图、颜色、CSS 圆都是**临时占位**。正式美术到位后，按本表替换，尽量只换资源、不改玩法代码。

摇杆交互对齐 [prototype-v5](../斗蛐蛐原型/prototype-v5/) 的反弹档：`style.css` 的 `#stick` / `#stick-base` / `#stick-knob`，`game.js` 里 `STICK_SIZE=128`、`STICK_VISUAL=38`。原型**没有摇杆贴图**，是 CSS 径向渐变。Unity 占位用同一套色和尺寸；正式图拖进 Theme 即可。

---

## 1. 资源目录总则

运行时图和 Prefab 按功能收进 `Assets/Resources/`。局内蛐蛐的骨骼、动作、换皮 Prefab **不进** `Resources`。脚本、场景、编辑器工具、TextMesh Pro、插件、Figma 源切、验收截图也不进 `Resources`。

运行时 Prefab 与正式图已按本节收到 `Resources`。必须在 Unity 编辑器里移动，保留 `.meta`，不要在资源管理器剪切。后文若仍写旧路径，以本节为准。

### 1.1 Scene 与 Prefab 归属

Player Build 只进四张 Scene：

| Scene | 路径 | 用途 |
| --- | --- | --- |
| `Login` | `Assets/Scenes/Login.unity` | 登录 |
| `MainMenu` | `Assets/Scenes/MainMenu.unity` | 大厅壳：底栏 + 当前页 |
| `Battle_Main` | `Assets/Scenes/Battle_Main.unity` | 对局外壳 |
| `Demo` | `Assets/Scenes/Demo.unity` | 战场世界，对局 Additive 加载 |

大厅页**不是** Scene。`MainMenu` 里按页切换 Prefab，不切场景。进出对局才 `LoadScene`。

大厅页（`Resources.Load` 路径，无扩展名）：

| 页 | Prefab | 说明 |
| --- | --- | --- |
| 主页 | 场景已实例化 `MainMenu/Prefabs/Parts/DouQuquMainMenu` | 村子主页。缺席时才加载 `MainMenu/Home/Prefabs/CricketHomepage` |
| 育虫盘 | `Merge/Prefabs/Canvas` | 底栏「养成」 |
| 图鉴 | `Collection/Prefabs/FigmaImport_cricket-collection_10_6` | 底栏「图鉴」（Figma `10:6` 蛐蛐谱） |
| 进战 | `BattleEntrance/Prefabs/BattleEntrance` | 底栏进战页；随机匹配 / 好友组队 |
| 准备 / 匹配 | `Matchmaking/Prefabs/FigmaImport_cricket-battle-royale_55_4` | 从进战页两个入口进入。组件在 `Matchmaking/Prefabs/Parts/`，贴图在 `Matchmaking/Textures/`，不再引用 `Assets/Figma/` |
| 商店 | `Shop/Prefabs/Shop` | 底栏「小铺」/ 主页商店（Figma `225:532` 百戏市集） |

覆盖层永远不是 Scene，盖在当前页或对局上：

| 覆盖层 | Prefab / 现状 | 说明 |
| --- | --- | --- |
| 活动介绍 | 运行时画 `ActivityPopupCanvas` | 主界面活动介绍 |
| 玩法规则 | 运行时画 `ActivityPopupCanvas` | 进战页；形式同活动介绍，标题不同 |
| 日勤任务 | `BattleEntrance` 内 `Popup_renwu` | 不切场景 |
| 蛐蛐详情 | `Collection/Prefabs/详情页` | 育虫盘 / 图鉴弹出 |
| 背包选虫 | `Matchmaking/Prefabs/Parts/选择名角背包` | 嵌在匹配页底部，不是单独覆盖层 |
| 结算 | `Settlement/Prefabs/Jiesuan` | 对局结束，sortingOrder 300 |

**不要再建、不要进包：** `Merge.unity`、`Collection.unity`、`Battle_Enter.unity`、`Battle_Matchmaking.unity`、`Preview/*`、`Game/UI/*/Scenes/*`。这些已从工程删除。前端改大厅 UI，打开对应 Canvas Prefab，不要为预览再新建 Scene。

Figma 重建菜单只写 Prefab，不要 `SaveScene`，不要改 Player Build。

`Assets/Scenes/FigmaBridgeTrial.unity` 与 `Assets/Figma/` 已删，不要再为预览新建 Scene。

编辑器菜单：`DouQuqu/Open Battle Scene`、`Open Lobby Scene`、`Ping Merge Prefab`。

### 1.2 局内蛐蛐（不进 Resources）

局内蛐蛐暂时不需要动态加载。骨骼、皮肤、材质和唯一运行时 Prefab 收在 `Art/Characters/`；动作单独放 `Animations/`；战斗逻辑仍在 `Scripts/DouQuqu/`：

```text
Assets/
├─ Art/Characters/
│  ├─ Rig/                           # 共享骨骼、Sprite Library（Unity 里生成）
│  ├─ Skins/
│  │  ├─ LAYER_NAMES.txt             # 22 个图层名，新皮肤必须一字不差
│  │  ├─ defaultCrickets.psb         # 骨骼真源 + 第 0 号皮肤
│  │  ├─ QingTou.psb                 # 之后每套皮肤一个 PSB，平铺在这里
│  │  └─ …
│  ├─ Materials/                     # 描边等材质
│  ├─ Shaders/
│  ├─ Cricket.prefab                 # 1 个可换皮 Prefab，不要 16 份，也不要单独建 Prefabs/
│  ├─ Cricket_Body.png               # 旧占位，PSB Prefab 接上后可删
│  └─ Cricket_Antenna.png
├─ Animations/Characters/Crickets/   # 一份 Animator + 动作，16 套皮肤共用
└─ Scripts/DouQuqu/                  # 蛐蛐战斗逻辑（现有位置，不要新建 Resources/Crickets）
```

原则：**一套骨架、一份动画、十六套皮肤。**

| 放这里                           | 放什么                                                     | 不要放什么                 |
| -------------------------------- | ---------------------------------------------------------- | -------------------------- |
| `Art/.../Rig/`                   | `CricketSkeleton.asset`、`CricketSkins` Sprite Library     | 单套皮肤的 PSB             |
| `Art/.../Skins/`                 | 各皮肤 PSB（图层名与 defaultCrickets 完全一致）            | 骨骼、Animator、Prefab     |
| `Art/Characters/Cricket.prefab`  | 1 个带 Sprite Skin + Sprite Resolver 的 Prefab             | 每种皮肤一份、`Prefabs/` 根目录 |
| `Animations/.../Crickets/`       | `Cricket.controller` 和 Idle / Charge / Jump 等 clip       | 按皮肤复制的动画           |
| `Resources/Crickets/`            | （不要建）                                                 | —                          |

新皮肤 PSB 命名：`Assets/Art/Characters/Skins/<Id>.psb`。`<Id>` 用英文或拼音，例如 `QingTou`、`YouHulu`。16 套齐了再在 Sprite Library 里加 16 个 Label。

**Photoshop 约束（换皮能不能套上骨架，全看这个）：**

- 图层名、数量、前后顺序与 `Skins/LAYER_NAMES.txt` 一致。
- 只改像素，不合并、不重命名、不加新部件。
- 身体比例尽量接近 Default，权重才能共用。
- 骨骼只在 Default 上改。改完后，其它皮肤在 PSD Importer 里勾选同一份 Skeleton Asset。

**Unity 里生成共享骨骼（只做一次）：**

1. 选中 `Skins/defaultCrickets.psb`，Inspector 确认 Character Rig 已开。
2. 在 `Art/Characters/Rig/` 创建 Skeleton Asset，拖回该 PSB 的 Skeleton 槽。
3. 新皮肤 PSB 导入后：Character Rig + 同一个 Skeleton Asset，再在 Skinning Editor 里按共享骨骼生成/拷贝权重。
4. 用 Default 的角色 Prefab 做一份 Sprite Library，每个部件一个 Category，每套皮肤一个 Label。运行时只换 Label，不换 Prefab。

局内优先通过场景直接引用，或在管理器、配置对象的序列化字段中拖入蛐蛐 Prefab；需要生成时使用 `Instantiate(cricketPrefab)`。Prefab 对 2D Animation 贴图、骨骼和动作资源的引用属于编辑器引用，不属于动态加载。

只有在蛐蛐种类或皮肤需要按配置 ID 临时选择、资源需要分包/远程更新，或资源量大到必须按需下载时，才考虑 `Resources` 或 Addressables。当前阶段先不要建立 `Resources/Crickets`。

顶视占位图仍在 `Resources/Battle/Entities/`（卵、幼虫、巢、饲料、道具、旧成虫 SVG）。正式骨骼角色以 `Art/Characters/Cricket.prefab` 为准，不要把顶视 SVG 再复制成 16 份 Prefab。

### 1.3 其余运行时资源：按功能放在 Resources 下

每个功能统一 `Prefabs/` + `Textures/`。已有代码加载路径的三块先不改名：`Fonts/`、`Merge/MergePhases/`、`Merge/MergeQualities/`。

```text
Assets/Resources/
├─ Fonts/                          # 已有，加载路径不要改
├─ Common/                         # 跨界面：底栏、金币、返回、共用图标
│  ├─ Prefabs/                     # 现有 BottomEventCarousel
│  └─ Textures/                    # 已建，跨界面图标尚未抽离
├─ MainMenu/                       # 主界面
│  ├─ Prefabs/                     # 村子主页 DouQuquMainMenu
│  ├─ Home/Prefabs/                # CricketHomepage 备页
│  └─ Textures/
├─ Merge/                          # 育虫盘
│  ├─ MergePhases/                 # 已有，加载路径不要改
│  ├─ MergeQualities/              # 已有，加载路径不要改
│  ├─ Prefabs/                     # Canvas + 育虫盘
│  └─ Textures/
├─ Collection/                     # 图鉴 + 蛐蛐详情
│  ├─ Prefabs/                     # 正式页 cricket-collection_10_6 + 详情页
│  └─ Textures/                    # 详情页切图；10_6 源切仍在 FigmaImports
├─ Matchmaking/                    # 准备 / 背包
│  ├─ Prefabs/                     # 匹配页 + Parts（player-2-zone / btn-ready / 选择名角背包 / 极品_1）
│  └─ Textures/                    # ArenaGarden / BackButton / CountdownRing / 槽位与卡片图
├─ Battle/                         # 战斗表现，不含角色骨骼
│  ├─ Board/                       # 战场环境：全屏背景 + 棋盘底 + 场地窗
│  ├─ Hud/                         # 战斗框体、头像、名字、UIToolkit
│  ├─ Lineup/                      # 进战编队（AdditionalScreens 63_5）
│  ├─ Entities/                    # 卵、幼虫、巢、饲料、道具、顶视占位
│  └─ Stick/                       # 摇杆 Theme + 底盘 / 杆头图
├─ BattleEntrance/                 # 进战页
│  └─ Prefabs/                     # BattleEntrance 正式页
├─ Shop/                           # 百戏市集
│  ├─ Prefabs/                     # Shop + ParchmentScrollCard / PriceButton / GoldDisplay
│  └─ Textures/                    # ShopBackground / CircleHelp / CardItemImage / CoinIcon / GoldCoins
├─ Activity/                       # 旧活动切图，仅备查
│  └─ Textures/
└─ Settlement/                     # 结算覆盖层
   ├─ Prefabs/                     # Jiesuan
   └─ Textures/
```

| 功能 | 目标 | 真源 |
| --- | --- | --- |
| 字体 | `Resources/Fonts/` | 不动 |
| 合成阶段 / 品质图 | `Resources/Merge/MergePhases/`、`MergeQualities/` | 不动 |
| 育虫盘 UI | `Resources/Merge/Prefabs/`、`Textures/` | 大厅页；不再单独建 Scene |
| 主界面 | `Resources/MainMenu/` | 正式页 `DouQuquMainMenu`；Home 备页在 `MainMenu/Home/`；切图在 `MainMenu/Textures/Figma/` 与 `FigmaImport/` |
| 图鉴 / 详情 | `Resources/Collection/` | 正式页 `FigmaImport_cricket-collection_10_6`；覆盖层详情页 |
| 准备 / 背包 | `Resources/Matchmaking/` | 正式页 + Parts 组件和贴图都在本目录，不再引用 `Assets/Figma/` |
| 棋盘底 | `Resources/Battle/Board/Textures/BattleBoard.png` | 加载路径 `Battle/Board/Textures/BattleBoard` |
| 全屏背景 / 场地窗 | `Resources/Battle/Board/` | 风景图 + `ArenaBackgroundScenery` / `Battlefield` Prefab |
| 战斗 HUD | `Resources/Battle/Hud/` | 框体、头像、名字、UIToolkit；不含背景和场地 |
| 进战编队 | `Resources/Battle/Lineup/` | AdditionalScreens 63_5 Prefab |
| 战场实体 / 摇杆 | `Resources/Battle/Entities/`、`Stick/` | 顶视占位；骨骼角色见 §1.2 |
| 进战 | `Resources/BattleEntrance/` | 正式页 `BattleEntrance` |
| 商店 | `Resources/Shop/` | 正式页 `Shop`；卡片 / 价格按钮 / 金币条和贴图都在本目录，不再引用 `Assets/Figma/` |
| 结算 | `Resources/Settlement/` | 对局覆盖层，不是 Scene |
| 共用底栏 | `Resources/Common/Prefabs/BottomEventCarousel` | 大厅 `LobbyNavCanvas` 挂一份；页内嵌套的底栏要关掉 |
| 共用图标 | `Resources/Common/Textures/` | 目录已建，尚未抽离 |

`Game/UI/` 只留编辑器 Builder、README 和验收截图，不再放运行时图或 Scene。`Assets/Figma/` 已删。`Assets/FigmaImports/` 只留进战页 / 图鉴仍嵌套引用的 Prefab 和切图，不要当大厅页。

---

## 怎么换（通用）

1. 文件放到表里的「放入路径」，覆盖或新建。
2. 在 Unity 里确认导入设置（见各节）。
3. 有 Theme / Prefab 槽的，把新图拖进对应字段。空槽 = 继续用占位。
4. 不要改玩法规则代码（跳跃、碰撞、投放、胜负）。

推荐格式：

| 类型                  | 格式                        | 说明                                           |
| --------------------- | --------------------------- | ---------------------------------------------- |
| 2D 角色 / 道具 / 摇杆 | **PNG，RGBA，正方形** | 透明底。也可用 SVG（项目已装 Vector Graphics） |
| UI 图标               | PNG 或 SVG                  | 透明底                                         |
| 地面 / 罐壁           | PNG 贴图 或 材质            | 地面要能 tile                                  |

PNG 建议 2 的幂（256 / 512 / 1024）。Sprite：`Texture Type = Sprite (2D and UI)`，Mesh Type = Full Rect，Pivot = Center。

---

## 2. 摇杆（优先）

**代码接口**

- 资源槽：`Assets/Resources/Battle/Stick/DouQuquStickTheme.asset`
- 两个 Sprite：底盘 / 杆头
- 绑定：战斗场景 `DouQuquDemoRuntime` → 触控输入的 Theme
- 换皮：改 Theme 上的 Sprite 后，运行时会自动用图；不必改 UXML

**放入路径（新建这些文件名即可）**

| 槽                 | 放入                                          | 建议像素             | 原型参考                                           |
| ------------------ | --------------------------------------------- | -------------------- | -------------------------------------------------- |
| 底盘 `baseSprite` | `Assets/Resources/Battle/Stick/Stick_Base.png` | 256×256 或 512×512 | 128px 圆盘，金边`rgba(196,165,116,.4)`，深褐径向 |
| 杆头 `knobSprite` | `Assets/Resources/Battle/Stick/Stick_Knob.png` | 128×128             | 52px 陶色圆头                                      |

两张都空着时：USS + Theme 里的占位色，观感接近原型。只换其中一张也可以。当前目录只有 Theme，没有正式底盘 / 杆头图。

**尺寸（相对竖屏短边，且不超过短边 20%）**

|              | 反弹     |
| ------------ | -------- |
| 底盘         | 88       |
| 杆头         | 36       |
| 头可移动半径 | 26       |
| 位置         | 点哪召出 |

**待办**

- [ ] 底盘正式图
- [ ] 杆头正式图
- [ ] 按住/蓄满是否要第二套杆头（未做槽，需要再加 `knobChargedSprite`）

---

## 3. 战场实体

Prefab：`Assets/Resources/Battle/Entities/Prefabs/`。图：`Assets/Resources/Battle/Entities/Textures/`。换图：覆盖 SVG/PNG，或改 Prefab 上的 Sprite Renderer。

成虫骨骼角色用 `Art/Characters/Cricket.prefab`，不要在 Entities 下再做青头 / 油葫芦各一份 Prefab。Entities 里的成虫 SVG 是顶视占位。

| 物体        | 当前占位                   | 放入路径                                                              | 建议                                 | 待办                                |
| ----------- | -------------------------- | --------------------------------------------------------------------- | ------------------------------------ | ----------------------------------- |
| 成虫 青头   | `DouQuqu_QingTou.svg`    | `DouQuqu_QingTou.png`（顶视占位）或 PSB 皮肤 `QingTou`               | 顶视 512²，身体外接圆约等于碰撞直径 | [ ] 正式顶视图 / 换皮               |
| 成虫 油葫芦 | `DouQuqu_YouHulu.svg`    | `DouQuqu_YouHulu.png` 或 PSB 皮肤 `YouHulu`                          | 同上                                 | [ ] 正式顶视图 / 换皮               |
| 幼虫        | `DouQuqu_Baby.svg`       | `DouQuqu_Baby.png` → `Prefabs/DouQuqu_Baby.prefab`                   | 256²                                | [ ]                                 |
| 卵          | `DouQuqu_Egg.svg`        | `DouQuqu_Egg.png` → `DouQuqu_Egg.prefab`                             | 256²                                | [ ] 头顶孵化胶囊是程序画的，另开 UI |
| 巢穴        | `DouQuqu_Nest.svg`       | `DouQuqu_Nest.png` → `DouQuqu_Nest.prefab`                           | 512²                                | [ ] 四档破损（现仅按 HP 染色）      |
| 饲料球      | `DouQuqu_Heart.svg`      | `DouQuqu_Heart.png` → `DouQuqu_Heart.prefab`                         | 256²                                | [ ]                                 |
| 增大        | `DouQuqu_Size.svg`       | `DouQuqu_Size.png`                                                   | 256²                                | [ ]                                 |
| 护盾        | `DouQuqu_Shield.svg`     | `DouQuqu_Shield.png`                                                | 256²                                | [ ]                                 |
| 蓄力强化    | `DouQuqu_Charge.svg`     | `DouQuqu_Charge.png`                                                | 256²                                | [ ]                                 |
| 合成棋子    | `DouQuqu_MergePiece.svg` | `DouQuqu_MergePiece.png`                                            | 256²                                | [ ] 按等级多图可后做                |

表现层只换 Prefab 引用，不碰模拟。

**待办（战场表现，无独立贴图槽）**

- [ ] 己方 / 敌方描边与地面圈
- [ ] 蓄力下蹲 + 地面红线（跳距预览）
- [ ] 耐力圆环（身周，3–8 格）
- [ ] 卵 / 幼虫头顶胶囊
- [ ] 护盾碎裂、击杀尘土（原型粒子）

---

## 4. 场地

红框里的棋盘是独立资源槽，和 HUD、蟋蟀逻辑分开。旧 Demo 地面 / 围栏已关掉。

|              | 当前                                  | 放入                                      | 待办       |
| ------------ | ------------------------------------- | ----------------------------------------- | ---------- |
| 棋盘底       | `Resources/Battle/Board/Textures/BattleBoard.png` | `battleTable-default.png`（1313×1982） | [x] 已放入 |
| 旧地面       | `DouQuquGround.mat` + Plane         | 不再显示                                  | —         |
| 罐沿         | `DouQuquBorder.mat`                 | 不再显示                                  | —         |
| 狂暴场地变色 | 无                                    | 可加第二张地面或颜色参数                  | [ ]        |

放入方法：把图命名为 `BattleBoard.png`，放到 `Assets/Resources/Battle/Board/Textures/`，覆盖即可，不用改代码。

场地逻辑尺寸不变：半宽 21.2、半深 31.8、圆角 7.2。

---

## 5. 大厅与覆盖层

| 界面 | 当前 | 放入 / 槽 | 待办 |
| --- | --- | --- | --- |
| 村子主页 | `MainMenu` 场景实例化 `DouQuquMainMenu` | 换 `MainMenu/Textures/` 或改 Prefab 引用 | [ ] 正式切图替换占位 |
| 首页备页 | `MainMenu/Home/Prefabs/CricketHomepage` | 仅主页缺失时加载 | 不必单独进流程 |
| 育虫盘 | `Merge/Prefabs/Canvas` + `育虫盘.prefab` | 棋盘底、格子、按钮 PNG | [ ] 正式合成 UI |
| 旧 UIToolkit 合成 HUD | `Battle/Hud/UIToolkit/DouQuquMergeHUD.uxml` | 大厅不再用；USS 还指向已删除的 `Assets/UI/` | 可删或标 Unused |
| 图鉴 | `Collection/Prefabs/FigmaImport_cricket-collection_10_6` | Figma `cricket-collection` / `10:6`；源切在 `FigmaImports/.../10_6/` | [ ] 卡片按收集点亮 |
| 蛐蛐详情 | `Collection/Prefabs/详情页` | 覆盖层，不切 Scene | [ ] |
| 进战 | `BattleEntrance/Prefabs/BattleEntrance` | 随机匹配 / 好友组队；日勤页内弹出 | [ ] |
| 活动介绍 / 玩法规则弹层 | 代码画色块 | 正式图做成 Prefab 后挂覆盖层 | [ ] 正式说明窗 |
| 准备 / 匹配 | `Matchmaking/Prefabs/FigmaImport_cricket-battle-royale_55_4` | 大厅页。Figma2Unity 标题/倒计时/返回 + Parts 四行/背包/确认选择；花园底图 `Matchmaking/Textures/ArenaGarden.png`。旧 `DouQuquMatchmaking` + `Prefabs/Leaf` 已删 | [ ] |
| 背包 | `Matchmaking/Prefabs/Parts/选择名角背包` | 嵌在 `FigmaImport_cricket-battle-royale_55_4` 底部；卡片 `极品_1` / 筛选项 `Frame7`。旧占位 `DouQuquMatchmaking_BackpackSelection` 与 `Backpack172_469` 已删 | [ ] |
| 结算 | `Settlement/Prefabs/Jiesuan` | 对局覆盖层 | [ ] 正式结算 |
| 战斗 HUD | `Battle/Hud/Prefabs/douququzhandou` | 框体、头像、名字 | [ ] |
| 进战编队 | `Battle/Lineup/` | 63_5 Prefab | [ ] |

---

## 6. 总待办（美术交接）

按设计文档「不写入功能文档」的美术项：

1. [ ] 摇杆三件套（底盘、杆头、圆心）
2. [ ] 两只虫贴图（青头 / 油葫芦）+ 己方敌方描边
3. [ ] 饲料球与三限时道具外形
4. [ ] 房子四档破损
5. [ ] 地面圈、观战虚线
6. [ ] 蓄力红线 / 下蹲
7. [ ] 耐力圆环
8. [ ] 首页、活动介绍、结算的正式切图（Prefab 槽已在，图多为占位）

换图联系：把文件放到上表路径，在 Theme 或 Prefab 上拖引用。缺图时占位继续能玩。

---

## 7. 现有资源盘点

盘点日期：2026-09-07

运行时资源已按功能收到 `Resources/`。`Game/UI/` 不再放运行时图。体积按当前磁盘文件，含源切 PNG。

| 类别 | 目录 | 数量 | 体积 | 用途 |
| --- | --- | --- | --- | --- |
| 战场实体占位 | `Assets/Resources/Battle/Entities/Textures/` | 13 SVG | 约 0.01 MB | 顶视成虫 / 幼虫 / 卵 / 巢 / 饲料 / 道具 |
| 摇杆 Theme | `Assets/Resources/Battle/Stick/` | 1 Theme | — | 底盘 / 杆头槽，图未到 |
| 战场环境 | `Assets/Resources/Battle/Board/Textures/` | 5 PNG | 约 22.7 MB | 棋盘底、全屏背景 |
| 战斗 HUD 图 | `Assets/Resources/Battle/Hud/Textures/` | 7 PNG + 1 SVG | 约 7.4 MB | 框体、头像、队伍图标 |
| 进战编队图 | `Assets/Resources/Battle/Lineup/Textures/` | 8 PNG | 约 3.3 MB | 63_5 |
| 合成阶段图 | `Assets/Resources/Merge/MergePhases/` | 3 PNG | 0.59 MB | `Merge/MergePhases/phase-N` |
| 合成品质图 | `Assets/Resources/Merge/MergeQualities/` | 16 PNG | 2.44 MB | 品质 1–4 × 变体 1–4 |
| 育虫盘切图 | `Assets/Resources/Merge/Textures/` | 6 PNG + 4 SVG | 3.46 MB | 背景、返回、金币 |
| 主界面切图 | `Assets/Resources/MainMenu/Textures/` | 4 PNG + 12 SVG | 8.29 MB | 村子 / Home |
| 图鉴正式页 | `Assets/Resources/Collection/Prefabs/FigmaImport_cricket-collection_10_6.prefab` | 1 Prefab | — | Figma `10:6` 蛐蛐谱，大厅加载这条 |
| 图鉴源切 | `Assets/FigmaImports/5jALrfLrmLV0NIOxaQURCu/10_6/layers/10_7.png` | 1 PNG | — | 图鉴页仍引用；其余 10_6 源切已删 |
| 图鉴切图 | `Assets/Resources/Collection/Textures/` | 详情页 Figma | — | 详情覆盖层 |
| 匹配切图 | `Assets/Resources/Matchmaking/Textures/` | 6 PNG | — | 花园 / 返回 / 倒计时环 / 槽位 / 卡片。背包用 `Parts/选择名角背包` 的 FigmaImage + `CricketCardArt.png` |
| 活动切图 | `Assets/Resources/Activity/Textures/` | 10 PNG + 6 SVG | 约 10 MB | 花脸 / 旧活动页 |
| 结算切图 | `Assets/Resources/Settlement/Textures/` | 1 PNG + 4 SVG | 0.18 MB | Jiesuan |
| 局内骨骼 | `Assets/Art/Characters/` | 1 PSB + Prefab | 约 3.1 MB | Default 皮肤与骨架 |
| 育虫盘验证图 | `Assets/Game/UI/BreedingBoard/Screenshots/` | 5 PNG | 6.57 MB | 验收截图 |
| 详情页预览 | `Assets/Game/UI/QuquXiangqing/Preview/` | 2 PNG | 0.61 MB | 整屏预览 |
| 过程截图 | `Assets/Screenshots/` | 13 PNG | 约 10.2 MB | 活动页 / 字体 / 耐力条等 QA 证据 |
| 仍在用的 Figma 源 | `Assets/FigmaImports/` | 进战 / 图鉴嵌套 Prefab + 少量 PNG | — | 只留 `BattleEntrance`、图鉴页还引用的文件 |

`Assets/Art/DouQuqu2D/` 已空，只剩空 `Stick/` 目录。旧 `Game/UI/zhandou/Textures/`、`FigmaImport/CricketUI/Textures/` 已搬走。

---

## 8. 运行时实体图（优先交给程序/美术）

全部位于 [`Assets/Resources/Battle/Entities/Textures/`](../Assets/Resources/Battle/Entities/Textures/)，当前为 SVG 占位。Prefab 在同级 `Prefabs/`（成虫共用 `DouQuqu_Cricket.prefab`，没有单独的青头 / 油葫芦 Prefab）：

| 文件                          | 类型    | 对应对象      |
| ----------------------------- | ------- | ------------- |
| `DouQuqu_Cricket.svg`       | 成虫    | 青头/默认成虫 |
| `DouQuqu_QingTou.svg`       | 成虫    | 青头          |
| `DouQuqu_YouHulu.svg`       | 成虫    | 油葫芦        |
| `DouQuqu_Baby.svg`          | 成长    | 幼虫          |
| `DouQuqu_Egg.svg`           | 成长    | 卵            |
| `DouQuqu_Nest.svg`          | 场景物  | 巢穴          |
| `DouQuqu_Heart.svg`         | 道具    | 饲料球        |
| `DouQuqu_Size.svg`          | 道具    | 增大          |
| `DouQuqu_Shield.svg`        | 道具    | 护盾          |
| `DouQuqu_Charge.svg`        | 道具    | 蓄力强化      |
| `DouQuqu_MergePiece.svg`    | 合成    | 合成棋子      |
| `DouQuqu_ChargeChevron.svg` | UI 特效 | 蓄力箭头      |
| `DouQuqu_ChargeFill.svg`    | UI 特效 | 蓄力填充      |

SVG 视图规格已统一为 128×128；`Nest` 为 160×160，`ChargeChevron` 为 80×80，`ChargeFill` 为 100×200。替换正式 PNG 时，沿用现有文件名和 `.meta`，或在 Unity 编辑器内替换引用，不要手改 `.prefab` / `.unity` 文本。

## 8.1 战场环境运行时图

正式图放在 [`Assets/Resources/Battle/Board/Textures/`](../Assets/Resources/Battle/Board/Textures/)。框体头像仍在 `Battle/Hud/Textures/`。

| 文件                           | 对应节点                   | 说明                                                                                          |
| ------------------------------ | -------------------------- | --------------------------------------------------------------------------------------------- |
| `ArenaBackgroundScenery.png` | `ArenaBackgroundScenery` | 1080×1920 全屏背景。菜单`DouQuqu/Assign Battle Background` 接到 Board Prefab |
| `BattleBoard.png`            | 世界空间棋盘               | 代码加载 `Battle/Board/Textures/BattleBoard`                                  |

替换时覆盖同名文件，保留 `.meta`。不要手改 `.prefab`。

---

## 9. 合成资源命名

- `MergePhases/phase-1.png`、`phase-2.png`、`phase-3.png`：阶段图，代码路径为 `Merge/MergePhases/phase-N`。
- `MergeQualities/quality-{品质}-{变体}.png`：完整矩阵为品质 1–4、变体 1–4，共 16 张；当前命名连续，无缺号。

---

## 10. 查重结果（仅截图）

以下是仍在磁盘上、内容相同或几乎相同的验证图。不影响运行时引用；是否保留由制作人按「最终证据 / 历史过程」决定：

| 重复组 | 文件 |
| --- | --- |
| B | `Assets/Game/UI/BreedingBoard/Screenshots/BreedingBoard_91_8_PlayVerified.png` 与 `_Latest.png` |
| 金币验收 | `BreedingBoard_91_8_BackpackGold_Verified.png` 与 `Verified-1.png` |

旧盘点里 `Assets/Screenshots/` 的多组完全重复、以及 `Game/UI/zhandou/Textures/Figma/CricketTeamCircle1/2/3.png`，目录已清空或搬走，不再列出。

---

## 11. 待确认项

1. 根目录 [`蛐蛐UI基础.png`](../../蛐蛐UI基础.png) 为约 10.75 MB 的大图，目前不在 `Assets/`，也没有 Unity `.meta`。建议确认它是设计源文件还是展示图；确认后再决定放入 `docs/` 参考目录或导出压缩版，暂不移动。
2. `Assets/Resources/Battle/Stick/` 目前只有 `DouQuquStickTheme.asset`，没有正式底盘/杆头贴图；摇杆继续使用 USS/程序占位。`Assets/Art/DouQuqu2D/Stick/` 已空，不要再往那里放图。
3. `Assets/Scenes/FigmaBridgeTrial.unity` 与 `Assets/Figma/` 已删。不要再为 Figma 预览新建 Scene。
4. `Assets/Screenshots/` 与各模块 `Screenshots/` 是验收记录，不建议和可运行贴图混用；新增截图请放到对应 UI 模块的 `Screenshots/`。
5. `Battle/Hud/UIToolkit/DouQuquMergeHUD.uxml` 仍引用已删除的 `Assets/UI/DouQuquMergeHUD.uss`。大厅育虫盘已改 uGUI Prefab，这份 UIToolkit 是否删除待定。

---

## 12. 交接建议

- 正式运行时图：沿用 `Assets/Resources/` 下按功能目录的现有命名，替换时保留 `.meta`。
- 大厅 UI：改 Prefab，不要新建 Scene，不要把预览 Scene 写进 Player Build。
- Figma 源切图：只放在各模块的 `Textures/Figma/`，不要从那里直接拖作最终资源，避免源图和运行时图混淆。
- Unity Figma Bridge 的 `Assets/Figma/` 已删。`Assets/FigmaImports/` 只留进战页 / 图鉴仍引用的 Prefab 和切图。
- 验证截图：放在对应模块的 `Screenshots/`，文件名加日期或 `Verified` / `Latest` 状态。
- 需要删除重复截图时，先保留带 `Verified` 或 `Latest` 的版本，并确认没有文档链接指向被删文件。
- 移动资源只在 Unity 编辑器里做，保留 `.meta`。

---

## 附：文件索引

- 本文：`docs/review/资源目录管理.md`（美术 / 资源交接，不是功能文档）
- 玩法：`docs/review/碰撞逻辑与配置_develop_v1.md`、`交互逻辑_develop_v0.md`、`道具系统_develop_v0.md`
- 摇杆原型：`docs/斗蛐蛐原型/prototype-v5/`
- 大厅壳 Scene：`Assets/Scenes/MainMenu.unity`
- 大厅页 Prefab：`Resources/MainMenu/`、`Merge/`、`Collection/Prefabs/FigmaImport_cricket-collection_10_6`、`Shop/Prefabs/Shop`、`Activity/`、`Matchmaking/`
- 覆盖层 Prefab：`Collection/Prefabs/详情页`、`Settlement/Prefabs/Jiesuan`；匹配背包是页内 `Parts/选择名角背包`
- 局内蛐蛐：`Assets/Art/Characters/Cricket.prefab`
- 历史设计稿：`docs/斗蛐蛐原型/design/`（只读，不改）
