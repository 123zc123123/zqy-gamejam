# 斗蛐蛐 · 对局模式

> 规定大厅进战入口、等待房、准备、胜负、观战、人机与巢穴测试。读者是策划与程序。
> 以本文与 `defaults.js` 为准。[prototype-v5](../斗蛐蛐原型/prototype-v5/) 仅作行为参考；冲突时以本文为准。
> 范围：如何进入对局、等待房怎么显、何时可蓄力、如何结束、出圈后如何观战。不含跳跃公式、手势、拾取投放、巢穴链、匹配页选虫。
> 时长与狂暴见 [道具系统](道具系统_develop_v0.md)。手势见 [交互逻辑](交互逻辑_develop_v0.md)。耐力见 [碰撞逻辑与配置](碰撞逻辑与配置_develop_v1.md) §4。好友组队细则见 [联机](联机_develop_v0.md)。匹配页己方三槽见 [匹配选虫](匹配选虫_develop_v0.md)。

## 1. 开场入口

大厅进战页提供两个平行入口。对局内无第二键。

| 入口 | 人数 | 对手 | 准备 3 秒 | 计时 / 胜负 |
| --- | --- | --- | --- | --- |
| 随机匹配 | 4 | 匹配到的玩家 | 有 | 正赛 + 加时；出圈即负 |
| 好友组队 | 4 | 同一房号的玩家 | 有 | 同随机匹配 |

- 入口名是「随机匹配」和「好友组队」。⚠️ 不用「开罐」「开始匹配」。
- 好友组队：房号在右，右侧是「确认」。点「确认」进房。房号是否校验 **待定**：现版点确认即进房。
- 点「随机匹配」：进入等待房，并进入匹配页。匹配页选己方三只虫，见 [匹配选虫](匹配选虫_develop_v0.md)。
- 好友组队点「确认」：进入等待房，不进匹配页。原两个入口的位置出现「准备」。点「准备」才进匹配页。等待房「准备」不是匹配页「准备就绪」。
- 旋钮开场可调，局内打开则暂停。

自己练、巢穴测试不在这两个入口里。Unity 大厅是否仍提供自己练 **待定**。

### 1.1 未进房 / 等待房

| 状态 | 随机匹配 / 好友组队 | 四人位 + 离开房间 | 「准备」 | 底栏 |
| --- | --- | --- | --- | --- |
| 未进房 | 显示 | 不显示 | 不显示 | 显示，可切页 |
| 随机匹配后 | 隐藏 | 一起出现 | 不显示 | 隐藏，已进匹配页 |
| 好友组队等待 | 隐藏 | 一起出现 | 显示在四人位下方 | 隐藏，不能切页 |

- 「离开房间」：退出等待房，回到未进房，「准备」一并隐藏。
- 等待房仍可点进度条看日勤，仍可点「玩法规则」。
- 好友组队的「准备」不是开局 3 秒倒计时，也不是匹配页「准备就绪」。点下去才进匹配页。

四人站位 **待定**。本地座位为己方。

### 1.2 玩法规则

点「玩法规则」弹出说明窗。形式与主界面「活动介绍」相同：遮罩 + 标题 + 正文 +「知道了」。

标题是「玩法规则」，不是「活动介绍」。点「知道了」或遮罩关闭。

### 1.3 日勤

点进度条（蛐蛐新手条 / 宝箱）在当前页弹出日勤，不切页。点「返回」或遮罩关闭。

「领取」只切按钮态：可领 → 已领取。不改进度数字、不发奖励。任务档位与数值 **待定**。

## 2. 准备倒计时

随机匹配与好友组队进入 3 秒准备：四人都不能蓄力、不能跳。倒计时结束起算正赛 0:00，并开始投放计时。

| 状态 | 能否蓄力 | 计时 |
| --- | --- | --- |
| 准备 | 否 | 不计入正赛 |
| 正赛 / 加时 | 停稳 | 计入 |
| 暂停 / 打开旋钮 | 否 | 不计入 |
| 出圈询问「继续看 / 退出」 | 否（对局暂停） | 不计入 |
| 观战中 | 自己不能操作 | 计入（对局继续） |

自己练、巢穴测试跳过准备，进场即可蓄力。

## 3. 胜负

仅随机匹配与好友组队结算。自己练、巢穴测试不结束对局。

1. 存活玩家只剩 1 人：该人胜。击杀记最后一撞；小蟋蟀记饲主。
2. 几乎同时出圈、场上无人：本局无胜者。
3. 硬截止仍有 2 人及以上：比地面投影到场心的距离，近者胜；距离相同比本局成长层；再同则平。
4. 中途有人离开或断线：本局作废。

出圈判定、护盾拉回见 [碰撞逻辑与配置](碰撞逻辑与配置_develop_v1.md) 与 [道具系统](道具系统_develop_v0.md) §5.4。正赛 / 加时 / 硬截止时长见道具系统 §2。

HUD：正赛显示距加时；狂暴显示距硬截止；场地变色。不弹说明窗。

## 4. 观战

自己出圈且场上仍有人时，对局暂停，出示「继续看 / 退出本局」。

| 选择 | 之后 |
| --- | --- |
| 继续看 | 解除暂停，对局打到结束；自己不能再蓄力 |
| 退出本局 | 本局对自己结束；对手若仍在场则继续 |

观战仍以本地座位为己方，描边与地面圈不变色。敌方主蟋蟀地面圈用虚线，小蟋蟀用实线。

出圈询问盖住摇杆。阵营色见文末美术交接，本文不定色值。

## 5. 人机

空位是否补人机 **待定**。补人机时对手用「贪蓄」。共用停稳、耐力、跳跃公式。人机自己选方向并按住计时，不读玩家摇杆反转。

决策顺序：

1. 贴边且仍向外滑 → 短跳拉回。
2. 否则有概率抢近处限时道具（狂暴中只看护盾）或饲料球。
3. 否则瞄准对手偏出圈方向。
4. 不预判小蟋蟀，不绕障。

> 来源：代码常量（不进面板）

| 旋钮 | 键名 | 默认 | 范围 | 作用 |
| --- | --- | --- | --- | --- |
| 蓄力倍率 | `chargeMul` | 1.2 | — | 对人蓄力时长的倍率 |
| 思考间隔 | `react` | 0.28 s | — | 松手后至下一次决策 |
| 预判位移 | `lead` | 0.22 | — | 瞄准对手时的位移提前量 |

巢穴测试对手为木桩，见 §7。

## 6. 自己练

场上仅自己。无倒计时、不计胜负、无人机。不在大厅两个入口里。

自己出圈：拉回开局位 `(0, -0.38×半深)`，速度清零，护盾与成长保留。对局不结束。

房子仍按道具系统投放。场上无敌人时，小蟋蟀做无定向短跳。

## 7. 巢穴测试

入口为同地址下的巢穴测试页，不在大厅两个入口里。

- 开场场心已有一栋房子。
- 两只都在场；未控制的那只为木桩，不跳。
- Tab 切换当前控制的蟋蟀。切走的立刻变为木桩，清除当前蓄力，不跳。
- 无倒计时，不按随机匹配 / 好友组队结算胜负。

## 8. 暂停与重开

- 空格暂停 / 继续。局内打开旋钮即暂停。
- R 重开：非联机立即再来一局；联机客机不能重开，须等房主再开。
- 重开对局才回满耐力，并清除成长、场上拾取物与巢穴。

## 附：文件索引

- 可调配置：正赛 / 加时时长见 [道具系统](道具系统_develop_v0.md)；人机三值为代码常量
- 行为参考：[prototype-v5](../斗蛐蛐原型/prototype-v5/)
- 操作说明：[怎么玩.txt](../斗蛐蛐原型/prototype-v5/怎么玩.txt)
- 进战页 Prefab：`BattleEntrance/Prefabs/BattleEntrance`
- 不是本系统：[交互逻辑](交互逻辑_develop_v0.md)、[碰撞逻辑与配置](碰撞逻辑与配置_develop_v1.md)、[道具系统](道具系统_develop_v0.md)、[联机](联机_develop_v0.md)、[匹配选虫](匹配选虫_develop_v0.md)
- 另开美术交接：己方 / 敌方描边与地面圈、观战虚线、上场虫贴图。不写入本文。
