diff --git a/斗蛐蛐原型/design/道具系统.md b/斗蛐蛐原型/design/道具系统.md
index fc03d19..b555c76 100644
--- a/斗蛐蛐原型/design/道具系统.md
+++ b/斗蛐蛐原型/design/道具系统.md
@@ -17,7 +17,7 @@
 5. [拾取物效果](#5-拾取物效果)
    - 5.1 [饲料球](#51-饲料球)
    - 5.2 [蟋蟀成长](#52-蟋蟀成长)
-     - [成长来源](#成长来源)
+     - [成长层数来源](#成长层数来源)
      - [成长效果](#成长效果)
      - [与增大的关系](#与增大的关系)
      - [清除时机](#清除时机)
@@ -91,7 +91,7 @@
 正赛结束双方仍在场内，进入加时：
 
 1. 全员获得蓄力强化，参数与拾取相同：读 `chargeScale`，同时改 $A_1$ 与 $T_{\max}$（见 §5.5）。
-2. 全员同时获得增大，参数与拾取相同：读 `sizeScale`。半径 $r$、质量 $m$ 先按当时成长层算完，再乘这个倍率。
+2. 全员同时获得增大，参数与拾取相同：读 `sizeScale`。半径 $R$、质量 $m$ 先按当时 `growRate` 算完，再乘这个倍率。
 3. 两样都持续到本局结束，不叠乘。若身上已有对应拾取效果，改成狂暴状态，去掉个人倒计时，倍率不变。
 4. 饲料球与道具继续按 §3 投放，节奏比正赛后段更密。
 5. 护盾不因进入加时而清除，按剩余时间或被消耗结算。
@@ -210,9 +210,9 @@
 
 - 成长只让这只蟋蟀变大、变重。
 
-#### 成长来源
+#### 成长层数来源
 
-- 蟋蟀成长是本局记在身上的层数，开局为 0。
+- 当前层数记为 `growLevel`，开局为 0。
 - 吃一颗饲料球加一层。
 - 把对手撞出圈也加一层。谁把他撞出去，谁加。
 - 上限读 `growMax`。
@@ -220,28 +220,39 @@
 
 #### 成长效果
 
-- 每多一层，半径和质量都按 `growPer` 相对开局放大一截。
-- 身体倍率 $= 1 + \text{蟋蟀成长} \times \texttt{growPer}$。碰撞半径 $= \texttt{BUG_R} \times$ 身体倍率。质量 $= m \times$ 身体倍率。
+半径按层数放大，质量和半径共用同一个 `growRate`：
 
-| 蟋蟀成长    | 身体倍率                                         | 碰撞半径            | 质量            |
-| ----------- | ------------------------------------------------ | ------------------- | --------------- |
-| 0           | 1                                                | `BUG_R`           | `m`           |
-| 1           | $1 + \texttt{growPer}$                         | `BUG_R` × 该倍率 | `m` × 该倍率 |
-| `growMax` | $1 + \texttt{growMax} \times \texttt{growPer}$ | 同上                | 同上            |
+$$
+R = R_0 \times \texttt{growRate} = R_0 \times (1 + \texttt{growLevel} \times \texttt{growPerRate})
+$$
+
+$$
+m = m_0 \times \texttt{growRate}
+$$
+
+| 量 | 含义 |
+| --- | --- |
+| `growLevel` | 当前成长层。开局 0，上限 `growMax` |
+| `growPerRate` | 每一层给 `growRate` 加多少，局前可调 |
+| `growRate` | 由上式算出，不是独立旋钮 |
+| $R_0$ | 开局碰撞半径，读碰撞文档，本文件不另配 |
+| $m_0$ | 开局质量，读碰撞文档，本文件不另配 |
 
-- 人变大：更好撞到别人，也更好被瞄准、更容易贴边。人变重：对撞更难被推飞。自己蓄力跳出去的速度不变。
-- ，不改变蓄力快慢，也不改变满蓄能飞多远。
-- 
+| `growLevel` | `growRate` | $R$ | $m$ |
+| --- | --- | --- | --- |
+| 0 | 1 | $R_0$ | $m_0$ |
+| 1 | $1 + \texttt{growPerRate}$ | $R_0 \times \texttt{growRate}$ | $m_0 \times \texttt{growRate}$ |
+| `growMax` | $1 + \texttt{growMax} \times \texttt{growPerRate}$ | 同上 | 同上 |
 
 - 每一层都加在开局体型上，不是吃一层就在当前体型上再乘一次。
-- 开局碰撞半径读碰撞文档 `BUG_R`，本文件不另配。
-- 开局质量读碰撞面板 `m`。若低于 0.08，按 0.08 算，避免质量为 0 时对撞公式除不尽。
+- 人变大：更好撞到别人，也更好被瞄准、更容易贴边。人变重：对撞更难被推飞。
+- 成长不改蓄力快慢，也不改满蓄能飞多远；自己跳出去的速度不变。
 
 #### 与增大的关系
 
 - 成长本局一直留着。
-- 增大只是限时再乘一截，到期后回到当前成长对应的身体。
-- 计算时先按层数得到半径和质量，身上有增大再乘 `sizeScale`。
+- 增大只是限时再乘一截，到期后回到当前 `growRate` 对应的身体。
+- 计算时先按公式得到 $R$、$m$，身上有增大再乘 `sizeScale`。
 
 #### 清除时机
 
@@ -255,14 +266,14 @@
 体积变大后更容易撞到对手，也更容易被瞄准，贴边更危险。自身起跳的 $\Delta v$ 不变——质量不进入蓄力，见碰撞 v2 §2.1。
 
 $$
-r' = r \times \texttt{sizeScale},\qquad m' = m \times \texttt{sizeScale}
+R' = R \times \texttt{sizeScale},\qquad m' = m \times \texttt{sizeScale}
 $$
 
-$r$、$m$ 取生效时已含成长的当前值。加时期间再吃饲料球：先加层，再乘仍在生效的 `sizeScale`。
+$R$、$m$ 取生效时已含成长的当前值。加时期间再吃饲料球：先加 `growLevel`，再乘仍在生效的 `sizeScale`。
 
 对撞分速度、重叠推开都用新质量。碰撞 v2 里抵抗随质量提高，更难被撞进失衡档，但身体也更大、更好被撞到。
 
-拾取到期后，$r$、$m$ 回到当前成长层对应的值，不是回到开局。速度不瞬间修改。若缩小后与对手重叠，按碰撞 v2 的质量比推开。狂暴结束即本局结束，不必还原。
+拾取到期后，$R$、$m$ 回到当前 `growRate` 对应的值，不是回到开局。速度不瞬间修改。若缩小后与对手重叠，按碰撞 v2 的质量比推开。狂暴结束即本局结束，不必还原。
 
 ### 5.4 护盾
 
@@ -273,7 +284,7 @@ $r$、$m$ 取生效时已含成长的当前值。加时期间再吃饲料球：
 地面投影刚越出有效区（碰撞 v2 的 `arenaSDF > 0`）且护盾仍在时：
 
 1. 护盾消失。
-2. 沿有效区法线向内移动中心，直到整只虫的碰撞圆回到区内，并再往里留 0.08（即 `arenaSDF = -(r + 0.08)`）。$r$ 取当时值（含增大与成长）。
+2. 沿有效区法线向内移动中心，直到整只虫的碰撞圆回到区内，并再往里留 0.08（即 `arenaSDF = -(R + 0.08)`）。$R$ 取当时值（含增大与成长）。
 3. 速度不变，按当前滑行档的摩擦继续匀减速（碰撞 v2 §3.3）。
 4. 不反弹，不拉回场地中心。再次出圈即出局。
 
@@ -313,7 +324,7 @@ $$
 
 ## 7. 计算顺序
 
-先按蟋蟀成长算出半径和质量，再乘增大（拾取或狂暴），再拿去对撞、做出圈判定、做护盾拉回。
+先按 `growRate` 算出 $R$、$m$，再乘增大（拾取或狂暴），再拿去对撞、做出圈判定、做护盾拉回。
 
 ---
 
@@ -325,7 +336,7 @@ $$
 | -------------- | --------------- | ------------------------- | --------- | -------------------------------------------------- |
 | 正赛时长       | `regTime`     | 90                        | 60–120 s | 到点未结束则进加时                                 |
 | 加时时长       | `otTime`      | 30                        | 15–45 s  | 狂暴持续时间；与正赛相加为硬截止                   |
-| 增大倍率       | `sizeScale`   | 1.30                      | 1.1–1.6  | 拾取与狂暴共用；$r$、$m$ 同乘                  |
+| 增大倍率       | `sizeScale`   | 1.30                      | 1.1–1.6  | 拾取与狂暴共用；$R$、$m$ 同乘                  |
 | 增大持续       | `sizeT`       | 6                         | 3–10 s   | 仅拾取；狂暴不读此项                               |
 | 护盾持续       | `shieldT`     | 8                         | 4–12 s   | 未被消耗也会到期                                   |
 | 蓄力倍率       | `chargeScale` | 1.25                      | 1.1–1.5  | 拾取与狂暴共用；$A_1 \times s$，$T_{\max} / s$ |
@@ -337,7 +348,7 @@ $$
 | 场上饲料球上限 | `heartCap`    | 6                         | 4–10     | 0:20 之后（含加时）                                |
 | 正赛补球间隔   | `heartGap`    | 7                         | 4–12 s   | 0:20–1:30                                         |
 | 加时补球间隔   | `heartGapOt`  | 5                         | 3–8 s    | 1:30 之后                                          |
-| 每层成长       | `growPer`     | 0.16                      | 0.02–0.5 | 每一层在开局体型上加多少；$r$、$m$ 同乘        |
+| 每层成长       | `growPerRate` | 0.16                      | 0.02–0.5 | 每一层给 `growRate` 加多少；$R$、$m$ 同乘        |
 | 成长上限       | `growMax`     | 6                         | 3–8      | 蟋蟀成长上限                                       |
 
 `itemTimes` 必须严格递增。改表只影响节奏。加时段内刷出的道具种类由规则锁成护盾，不单独配池。
@@ -353,7 +364,7 @@ $$
 | 到点上一颗仍在                            | 再出一颗，不替换       |
 | 刷新距边 / 距角色 / 距饲料球 / 距其他道具 | 2.4 / 3.0 / 1.6 / 2.0  |
 | 优先刷新带（距原点）                      | 6～12                  |
-| 护盾拉回余量                              | 当时$r + 0.08$       |
+| 护盾拉回余量                              | 当时 $R + 0.08$       |
 | 正赛结束后道具池                          | 仅护盾                 |
 
 ---
